version: 0.2

phases:
  pre_build:
    commands:
      - pip install -r requirements.txt
      - echo "🚀 Compilando... ✨"
  build:
    commands:
      - python -m unittest discover
artifacts:
  files:
    - '**/*'
